# Optional. The deployed form defaults to the activated server-side FormSubmit
# endpoint for yulaversestudio@gmail.com. Set this only when replacing it with a
# compatible application/x-www-form-urlencoded delivery endpoint.
CONTACT_DELIVERY_URL=
